- if flash
  - flash.each do |type, message|
    %div{class: "text-center alert #{alert_class_for(type)} alert-dismissible", role: "alert"}
      %button.close{"data-dismiss" => "alert", :type => "button"}
        %span{"aria-hidden" => "true"} ×
        %span.sr-only Close
      %strong
        = type.humanize
        !
      = message
